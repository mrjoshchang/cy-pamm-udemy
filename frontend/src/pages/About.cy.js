import React from 'react';
import AboutPage from './About';
/* global cy */

describe('<AboutPage />', () => {
  it.only('do nothing yet', () => {
    // see: https://on.cypress.io/mounting-react
    cy.mount(<AboutPage />);
  });
});
